export interface DashboardData {
  title: string;
  description: string;
}

export interface Meta {
  totalCount: number;
}

export interface Todo {
  id: number;
  content: string;
  completed: boolean;
}
